pipeline {
    agent any 
    environment {
        AWS_ACCESS_kEY_ID = credentials('jenkins-aws-secret-key-id')    
    }
    stages {
        stage('Example Stage1') {
            steps {
            }
        }
    }
}
