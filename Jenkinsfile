pipeline {
    agent any 
    stages {
        stage('Deploy') {
            steps {
                def singlyQuoted = 'Hello'
                echo "Helloは${singlyQuoted}"
            }
        }
    }
}
